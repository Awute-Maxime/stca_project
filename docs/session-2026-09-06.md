# Session 2026-09-06 — Remise à niveau de la documentation : pointeurs périmés, 11 journaux reconstruits, extraction du design system

Session sans code applicatif : **entièrement consacrée au contexte et à la documentation**, à la
demande de l'utilisateur qui reprenait le projet dans un nouvel outil et craignait de « perdre le fil
et le contexte ».

## Déroulé

1. **Audit de continuité** — vérifier ce qui survit réellement d'une session à l'autre.
2. **Correction de 2 pointeurs périmés** (`CLAUDE.md`, `regles.md`).
3. **Découverte d'un trou de 5 semaines** dans les journaux de session.
4. **Reconstruction des 11 journaux manquants** depuis le transcript brut.
5. **Durcissement de la règle 3** pour que le trou ne se reproduise pas.
6. **Deux livrables** demandés par l'utilisateur : stack technique + extraction du design system.

## Ce qui est fait

### 1. Pointeurs périmés corrigés

| Fichier | Correction |
|---|---|
| `CLAUDE.md` | 🔴 pointait vers **la mauvaise mémoire** — `…\projects\C--Users-MaxFox-II\memory\`, un dossier **figé au 7 juin 2026** (6 fiches). La mémoire vivante est `F--AI-PROJECTS` (25 fiches). Corrigé aux 3 endroits, avec un avertissement explicite pour qu'aucune session future n'y retourne. |
| `regles.md` — contexte | Nom **TCIT** (au lieu de STCA-Electron), identifiants réels (`tcit-desktop`, `tg.tcit.desktop`), suite des 3 apps, fichiers partagés `%PROGRAMDATA%\TCIT\` |
| `regles.md` — stack | Remplacée par la stack **vérifiée dans `package.json`** : Prisma + SQLite, Zustand, `ws`, Vitest. **Ni Express ni PostgreSQL** aujourd'hui (Phase 4) |
| `regles.md` — règle 12 | Disait « **PREMIUM DARK uniquement** » (palette `#080f1d` / `#4F9CF9`) — **en contradiction directe** avec le travail de 2026-08/09. Réécrite en « clair premium (référence) + sombre additif », avec le tableau des vraies variables `--tc-*` relues dans `index.css`. Ancienne palette conservée en note d'historique avec mention « ne pas ressusciter ». |
| `regles.md` — plan | Tableau figé au 10/06 remplacé par l'état réel + les 5 chantiers ouverts |

**Aucune des règles validées 1→11 et 13→19 n'a été touchée** (règle 15).

### 2. Le trou dans les journaux — constat

| | |
|---|---|
| Dernier journal existant | `session-2026-07-28-pointage-build.md` |
| Période sans journal | **29 juillet → 3 septembre** |
| Commits non racontés | **61** |
| Jours de travail concernés | **11** |

Cause : la règle 3 n'était déclenchée que par le mot-clé « sauvegarde ». La période la plus dense du
projet (décodeur VIN, personnalisation, **tout le mode sombre**, installeurs) n'avait aucun récit.

Le contexte n'était pas perdu pour autant — il avait migré vers la mémoire (25 fiches), les plans et
les messages de commit. **Ce qui manquait, c'était le *pourquoi*.**

### 3. Les 11 journaux reconstruits

Source : le transcript brut `…\.claude\projects\F--AI-PROJECTS\12ee14eb-….jsonl` — **265 Mo,
36 184 lignes, 34 jours de conversation** (7 juin → 3 septembre). Les 11 jours manquants y
représentaient **172 messages réels de l'utilisateur**.

Méthode : script Node en flux (`readline`) → extraction par jour des paroles de l'utilisateur, des
extraits de réponses et des fichiers touchés → croisement avec `git log` du jour → rédaction.

| Journal | Sujet | Commits |
|---|---|---|
| 29/07 | Phase 7 `tcit-ui` + refonte top bar Affichage | 6 |
| 30/07 | Décodeur VIN : diagnostic + secours NHTSA | 3 |
| 31/07 | Décodeur « vivant » — 4 passes de correction | 4 |
| 01/08 | **Efficacité VIN — TRIPLE 78,1 %** | 9 |
| 03/08 | Carrosserie / motorisation / segment | 3 |
| 05/08 | Comportement des fenêtres (modal / flottante) | 2 |
| 06/08 | Checklist de revue + splash Affichage/Pointage | 4 |
| 11/08 | **Personnalisation + mode sombre** (plans A, B, A-bis) | 15 |
| 12/08 | Sombre : Enregistrement, Liste, Recherche | 3 |
| 14/08 | Sombre : 6 lots d'écrans | 6 |
| 03/09 | **Sombre terminé + 3 installeurs livrés** | 16 |

Chaque fichier porte un bandeau ⚠️ indiquant qu'il est reconstruit, et cite **les phrases exactes de
l'utilisateur** aux moments de décision. `docs/session-*.md` est désormais **continu du 6 juin au
6 septembre** — 33 fichiers.

### 4. Règle 3 durcie

Scindée en **3.a — écriture systématique** (4 déclencheurs : « sauvegarde », un commit produit, 90 %
de contexte, ou l'utilisateur annonce qu'il s'arrête) et **3.b — commande `sauvegarde`** (l'ancienne
procédure). Format imposé + avertissement daté sur ce qui vient d'arriver.

### 5. Deux livrables sur le Bureau (emplacement temporaire voulu par l'utilisateur)

**`TCIT - Stack technique.txt`** — 11 sections, toutes les valeurs relues dans `package.json`,
`prisma/schema.prisma` et `src/`. Inclut les 12 modèles Prisma, la CSP et sa conséquence, les
5 pièges de packaging, les chiffres mesurés du décodeur VIN, et les 4 points 🔴 de sécurité.

**`TCIT - Design System/`** — 16 fichiers, 5 dossiers, 125 Ko. CSS et TS **extraits par script**
depuis les sources (pas recopiés) : 38 lignes de variables, 27/27 keyframes, 10/10 blocs
`prefers-reduced-motion`, zéro doublon. Contient un **styleguide vivant** `apercu.html`
(bascule clair/sombre, composants réels animés).

### 6. `.gitignore`

Ajout de `electron.vite.config.*.mjs` (temporaires electron-vite) et `_*.mjs` / `_*.html`
(scripts de pilotage CDP jetables), qui traînaient dans le dépôt.

## Vérifications

- Styleguide `apercu.html` **testé à l'écran**, en clair **et** en sombre (bascule fonctionnelle,
  valeurs de jetons mises à jour dynamiquement, composants et animations corrects).
- Extraction CSS contrôlée par comptage contre la source (keyframes, `prefers-reduced-motion`,
  doublons).
- Working trees des 4 dépôts vérifiés.

## Corrections apportées en cours de session

- **Dates fausses** : les fichiers avaient d'abord été datés du **04** et du **05/09** alors que nous
  sommes le **06**. Corrigé dans les 12 fichiers du dépôt et dans les 2 livrables du Bureau.
- **Bug de l'extracteur** : le filtre de sélecteurs testait `/^:root\s*\{/` alors que le sélecteur
  découpé ne contient pas l'accolade → `tokens.css` sortait vide (0,7 Ko). Corrigé en comparaison
  stricte, le fichier est passé à 3,7 Ko avec les 2 blocs complets.

## Découvertes notables

- Le travail de **splash du 06/08** (Affichage et Pointage) était resté **non committé pendant près
  d'un mois** — découvert par hasard le 03/09. Symptôme direct de l'absence de journal.
- Le **14/08**, une limite de dépense avait été annoncée à tort à l'utilisateur, interrompant la
  session sans raison. Consigné dans le journal du jour.

## Commits

Voir le commit de sauvegarde de cette session.

## Prochaine étape

Reprendre les chantiers applicatifs — au choix : plan C (`tcit-ui` → Affichage/Pointage),
checklist de revue 🔴, ou calibrage fin des couleurs d'accent.
En attente également : le retour de test du ZIP `TCIT-Test-0.9.0` par l'utilisateur et son collaborateur.

## ⚠️ Correction apportée après coup (même jour)

En listant les « prochaines étapes », j'ai annoncé qu'il restait à **créer** la facture, la fiche ID
et les feuillets d'assurance. **C'est faux — ils existent tous depuis le 25/07/2026** (commit
`a9db57a`, « famille documents complète ») : `CarteGrise.tsx`, `Facture.tsx`, `FicheId.tsx`,
`Feuillet1.tsx`, `Feuillet2.tsx`, `Feuillet3.tsx`, chacun avec sa fenêtre d'aperçu
(`apercu.facture`, `apercu.ficheId`, `apercu.feuillet1/2/3`).

**Cause :** j'ai repris la ligne `description` périmée de la fiche mémoire
`project_stca_next_task` sans la vérifier dans le code. **L'utilisateur a corrigé** — c'est lui qui
avait raison.

**Ce qui reste réellement sur les documents :** les **validations utilisateur** et le **calibrage des
positions en mm** sur imprimante réelle (bloqué faute d'imprimante physique).

**Leçon :** une fiche mémoire est une observation datée, pas l'état courant. Vérifier dans le code
avant d'annoncer qu'une fonctionnalité manque.
