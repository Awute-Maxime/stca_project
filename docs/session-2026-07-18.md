# Session du 18 juillet 2026 — Documents d'impression : Facture + Fiche ID jaune

Tous les travaux testés en Electron réel, commis et poussés.

## 1. FACTURE (`7250d0c`)
Reproduction du PDF réel fourni (FACTURE.pdf) :
- `Facture.tsx` : document **A4 complet** en deux volets — **STCA** (en-tête arrondi, Facture n°/Du, Nom+pays, Destination code+immat, N° Chassis+marque, Nature Véh., Montant 10 000) et **POOL TPVM-VT** (coordonnées, code-barres, Montant assurance 12 000) ; **Montant Total** encadré ; « 0 dont 0 » + TCIT-année ; **NB** souligné. PrintCss A4 + `FacturePrintDirect`.
- `FactureApercuWindow` (`apercu.facture`) : autoPrint → signal `tcit_facture_printed` → auto-fermeture.
- **Impression MULTI-DOCUMENTS** dans Edition Documents : facture pour « Tous », « Facture + CG », « Uniquement Facture » ; file directe séquentielle ; Prévisualiser attend TOUS les signaux.

## 2. Fix duplicata depuis la LISTE + bouton 👁 Aperçu (`61d14fc`)
Signalé par l'utilisateur : seule la Recherche avait été branchée.
- `editionHelpers.ts` : helper PARTAGÉ (docsPourChoix, cgDataDe, factureDataDe, ficheIdDataDe, ouvrirApercuDoc) — source unique Liste/Recherche.
- Bouton **👁 Aperçu** ajouté au modal EditionDocsModal (WinDialogs).
- ListePage entièrement branchée (aperçu / prévisualiser / direct).

## 3. FICHE ID JAUNE (`b1a03a0`)
Reproduction de la photo réelle — 10,5 × 21,2 cm, **entièrement imprimée** (accompagne la carte grise pré-imprimée ; ses codes-barres permettent de retrouver les infos si la CG s'efface) :
- `Code128.tsx` : **vrai encodeur Code 128-B** (motifs officiels + checksum mod 103) en SVG — codes-barres réellement scannables, zéro lib.
- `FicheId.tsx` : positions mm relevées sur le modèle (STCA + Le:, code-barres haut, nom, pays, châssis, marque, transit, N° Immat./N° Tri/Du, code-barres bas, texte « remplace la photocopie », pied de page).
- `FicheIdApercuWindow` (`apercu.ficheId`) : fond jaune papier, signal `tcit_ficheid_printed`.
- Branchée partout : options « Tous », « CG + Fiche ID » (2), « Uniquement Fiche ID » (6) ; duplicata Liste + Recherche ; file directe facture → cg → ficheId.

## 4. Encodage des codes-barres (`ba6dc21` + `4f2633b`)
Scan de la fiche réelle par l'utilisateur : les 2 codes-barres du vrai STCA contiennent la même chose (immat+frontière, ex. Y9209CK). Choix validés :
- **HAUT = châssis (VIN)** — suggestion utilisateur, plus de répétition.
- **BAS = immat+frontière | N° Tri | clé d'authentification** (4 caractères FNV-1a dérivés immat+châssis+numTri+secret) — retrouve le N° Tri effacé ET détecte une fiche falsifiée. `cleAuthentification()` exportée pour une future fonction « Vérifier une fiche » par scan.
- **Preuve par décodage barre-par-barre** (script capable de lire le SVG comme une douchette) : HAUT = `2T3P1RFV7KW054154` ✓, BAS = `Y9209CK|021094|ZVES` ✓, checksums valides.

## 5. FEUILLET N°3 CONDITIONS PARTICULIÈRES — Blanc A4 pré-imprimé (`ecfaff8` + `d4cf06b` + `7365733`)
Modèles fournis : PDF des données imprimées par STCA II + photo du feuillet POOL TPVM-VT GIE rempli. Même principe que la carte grise (données seules positionnées en mm sur A4) :
- `Feuillet3.tsx` : N° Police « 1 - {réf} / {AAAAMMJJ} », effet/échéance (+14 jours), parc, souscripteur, véhicule, immatriculation STAC « TG WZ Y 9209 CK », 7 primes du modèle (RC 5 065, CEDEAO 506, IC 3 750, Total 9 321, Accessoires 2 000, Taxes 679, TTC 12 000 = montant assurance facture), « Reçu de X la somme de … Francs CFA » avec `montantEnLettres` (français ≤ 999 999).
- **DEUX exemplaires identiques** à l'impression (le PDF modèle a 2 pages) — break-after page, chaque exemplaire avec son ombre à l'écran.
- **Mention DUPLICATA stricte** : uniquement en réédition DUPLICATA — jamais à la 1re impression ni en Renouvellement (mention « RENOUVELLEMENT » reportée au besoin, une ligne).
- `Feuillet3ApercuWindow` (`apercu.feuillet3`, signal `tcit_feuillet3_printed`) ; options « Tous », « Toutes Assurances », « Feuillet N°3 » (0, 3, 9) ; duplicata Liste + Recherche ; file directe facture → cg → ficheId → feuillet3.
- Testé en réel : aperçu conforme au PDF (positions, primes, « douze mille », échéance 18/07→01/08), 2 exemplaires, DUPLICATA présent/absent selon le cas.

## En attente / reprise (prochaine session : les autres feuillets)
- **PROCHAINE SESSION : Feuillets N°1 (bleu) et N°2 (rose)** — modèles à fournir par l'utilisateur (probablement même gabarit que le N°3).
- Utilisateur : re-scanner le code-barres HAUT de la fiche ID sur un aperçu frais — attendu : le châssis.
- Validations générales : Facture + Fiche ID + Feuillet N°3.
- Fenêtres config Outils : vraie persistance (modèle adminConfig).
- Idées notées : fonction « Vérifier une fiche » (scan → authentification) ; mention « RENOUVELLEMENT » au besoin.
- Matériel : imprimante (calibrage CG + CSS print Liste + pagination 2 exemplaires du Feuillet 3), clé USB (clef d'administration).
