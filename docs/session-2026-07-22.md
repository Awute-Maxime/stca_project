# Session du 22 juillet 2026 — Types Véhicule + Paramètres Destinations (VALIDÉES)

Deux fonctions du menu Outils+Config. livrées, testées en Electron réel et
validées par l'utilisateur. Suite logique de Configuration Assurances.

## 1. Types de Véhicule — « Liste des Types de Véhicules (pour assurances) »
Capture réelle fournie. L'utilisateur a précisé que ces 3 types
(**Voiture / Camion / Autre**) sont EXACTEMENT ceux sélectionnés à
l'Enregistrement ET les catégories de Config. Assurances → décision validée :
**source unique liée** + fenêtre **éditable**.

- **`typesVehiculeStore.ts`** (NOUVEAU, `tcit_types_vehicule`) : `TypeVehicule
  {id, rang, nom}`, défaut Voiture/Camion/Autre, hooks get/set/use +
  `nomsTypesVehicule()`.
- **Lien automatique → Config. Assurances** (`reconcilierAssurances`) : à chaque
  enregistrement, chaque assureur garde 1 tarif par type dans l'ordre du rang.
  Ajout → tarif par défaut (13000/679/20) ; suppression → retiré ;
  **réordonnancement → l'ordre des tarifs suit le rang** (bug de détection
  corrigé en cours de test : comparaison de la séquence complète, pas juste la
  longueur).
- **Fenêtre** refaite fidèle à la capture (colonnes « Rang » + « Nom ou type de
  véhicule », lignes de remplissage) mais éditable : renommer inline, ▲▼
  réordonner, 🗑 supprimer, champ Ajouter, Enregistrer (vert si modif), Fermer.
- **En-tête clair maison** (#EEF3FB, texte bleu marine majuscules) après feedback
  utilisateur (« les en-têtes bleues empilées ne m'intéressent pas ») → Règle 21.
- Garde admin (`outils.typesVehicule` dans FENETRES_ADMIN).
- **Enregistrement branché** : le menu « Véhicule à assurer » lit le store
  (3 types au lieu de 6 codés en dur). Testé E2E.

## 2. Paramètres Destinations — « Liste des destinations »
3 captures fournies (liste + Nouveau + Modifier). **Nouveauté TCIT demandée par
l'utilisateur** : la **couleur de la plaque pré-imprimée** liée à chaque
destination, éditable (les plaques sont pré-fabriquées par couleur ; si le
fournisseur change une couleur, l'admin l'ajuste sans toucher au code).

- **`destinationsStore.ts`** (NOUVEAU, `tcit_destinations`) : `DestinationParam`
  = MockDestination + `couleur`. Défaut = mockDestinations + couleurs reprises
  des anciens DEST_COLORS. Hooks `useDestinations`/`useDestColors`, getters
  `getDestinations`/`getDestColors`/`couleurDe`, `upsertDestination`/
  `removeDestination`, `PALETTE_PLAQUES` (8 couleurs standard).
- **SOURCE UNIQUE des couleurs** : avant, `DEST_COLORS` était **recopié en dur
  dans 8 fichiers** (Dashboard, Liste, Recherche, Enregistrement, Pointage,
  Analyse, AssuranceWindows, ListePrintPreview) → tous refactorés vers
  `useDestColors()`. Idem `mockDestinations` → `getDestinations()`
  (AnalysePrintPreview, DashboardHome, PointagePage, Enregistrement).
- **Fenêtre** fidèle à la capture 1 (Code Destination | Tarif | Destination |
  Lettre | N° immatriculation) + colonne **Couleur** (pastille), en-tête clair
  maison, boutons Nouveau / Modifier / Supprimer / Imprimer / Fermer. Suppression
  bloquée si des véhicules existent (Modal.confirm sinon).
- **Fenêtre d'édition indépendante** `edition.destination` (Règle 10, comme
  edition.assureur) : champs de la capture 2/3 + bloc **« Couleur de la plaque
  pré-imprimée »** = palette de 8 couleurs + option **Perso** (`<input
  type=color>`) + aperçu du code hex. Valider → upsertDestination → sync auto.
- **Aperçu impression** `apercu.listeDestinations` (bouton Imprimer, A4 fidèle
  au modèle liste assurances, avec pastille couleur).
- Garde admin (`outils.paramDestinations`). WINDOW_REGISTRY : titre « Liste des
  destinations » 760×560 ; edition.destination 520×560 ; apercu 880×860.

## Tests réels (CDP)
- Types : défaut Voiture/Camion/Autre, ajout/suppr/réordre + réconciliation
  assurances, Enregistrement montre les 3 types, en-tête clair validé
  visuellement.
- Destinations : couleurs par défaut ; changement AFO→bleu/NO→orange (store) puis
  AFO→rouge via le **vrai bouton Valider** → liste mise à jour EN DIRECT ;
  palette + perso ; Enregistrement (types + destinations) intacts ; aucune erreur
  console liée. Stores laissés propres.

## Règle 21 mémorisée
« Toujours se référer à ce qu'on a déjà fait pour garder les styles cohérents. »
En-tête de tableau clair maison réutilisé pour Types Véhicule ET Destinations.

## Prochaine session
Fixer N° Référence + Config. Poste N° IMMAT., puis Exporter, Pointage.
(Restent aussi les validations d'imprimante physique en attente.)
